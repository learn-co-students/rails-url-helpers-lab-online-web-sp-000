<!--
<div>
  <% @students.each do |student| %>
    <div><a href='<%= "/students/#{student.id}" %>'><%= student.to_s%></a></div>
  <% end %>
</div>
-->
<!--
<div>
  <% @students.each do |student| %>
    <div><%= link_to student.to_s, "/students/#{student.id}" %></a></div>
  <% end %>
</div>
-->
<!--
<div>
  <% @students.each do |student| %>
    <div><%= link_to student.to_s, student_path(student.id) %></div>
  <% end %>
</div>
-->
<div>
  <% @students.each do |student| %>
    <div><%= link_to student.to_s, student_path(student) %></a></div>
  <% end %>
</div>