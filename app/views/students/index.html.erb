<div>
  <% @students.each do |student| %>
    <div>

  <a href = '<%= "/students/#{student.id}" %>'>    <%= student.to_s %></div> </a>


  <% end %>
</div>

<div>
  <% @students.each do |student| %>

    <%= link_to student.to_s, student_path(student) %>

  <% end %>
</div>
