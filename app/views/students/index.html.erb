<div>
  <%= @students.each do |student| %>
        <div><%= @student.to_s %></div>
        <%link_to "@student.to_s", "students_path(@student)"%>
        <%link_to "profile", "students_path(@student)"%>
        <%= link_to "Activate", "activate_student(@student)" %>
        <%= link_to "Deactivate", "deactivate_student(@student)" %>  
        <%end%>
</div>

