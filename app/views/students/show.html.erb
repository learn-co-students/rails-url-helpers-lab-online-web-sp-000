<h1><%= @student.to_s %></h1>

<%# Read whether the set @student is active %>
<%# If "active" return active message %>
<%# Else return inactive message %>

<% if @student.active %>
     This student is currently active.
<% else %>
     This student is currently inactive.
<% end %>       